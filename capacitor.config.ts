import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.new.app.get',
  appName: 'photo-gallery',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
